{
  "name": "owe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --port 8787",
    "deploy": "wrangler pages deploy public --project-name owe --branch main",
    "db:migrate:local": "wrangler d1 migrations apply owe-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply owe-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.18.0"
  }
}
